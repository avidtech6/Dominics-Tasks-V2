# Invariants — DominicsTasks

Mirrors `app-pact.md` Invariants section for cross-reference.

1. Behaviour singletons stable
2. Task IDs unique per session
3. Soft-delete preserves data
4. Family has at least one parent
5. Chat timestamps monotonic per session
6. Routes resolve to exactly one component
